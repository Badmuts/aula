FROM node:8.12-alpine

WORKDIR /usr/src/app

COPY package*.json lerna.json ./
COPY packages/app-web ./packages/app-web

RUN npm install --loglevel notice --unsafe-perm

CMD ["npm", "--prefix", "packages/app-web", "start"]
